name: Oura dashboard tests

on:
  push:
    paths:
      - "oura-dashboard/**"
      - ".github/workflows/oura-ci.yml"
  pull_request:
    paths:
      - "oura-dashboard/**"
      - ".github/workflows/oura-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: python -m pip install --upgrade pip pytest

      - name: Run tests
        run: python -m pytest -q
        working-directory: oura-dashboard

      - name: Check the demo build works end to end
        run: python -m oura_dashboard demo --days 90
        working-directory: oura-dashboard/src
